/**
 * Submit Edit as Votable
 */
(function submitEditVotable() {
    document.querySelector('.make-votable, [name*="make_votable"]')?.click();
    document.querySelector('#enter-edit, button.submit, #submitAliases')?.click();
})();
