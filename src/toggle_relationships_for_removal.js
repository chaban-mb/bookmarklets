/**
 * Toggle Relationships for Removal
 */
(function toggleRelationshipsForRemoval() {
    document.querySelectorAll('.relationship-list .remove-item').forEach((el) => {
        el.click();
    });
})();
